const names = ['Edoardo', 'Simone', 'Francesco'];

// Stampa in console tutti i nomi
// Risultato: 'Edoardo', 'Simone', 'Francesco'

//quando da un array voglio prendere in considerazione tutti gli elementi faccio foreach
// names.forEach(function(nomi) {
//     console.log(nomi)  
// });

// Itero su ogni elemento dell'array "names"
names.forEach((nomi) =>
    // Stampo ogni nome
    console.log(nomi)  
);

